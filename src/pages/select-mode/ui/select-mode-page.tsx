/**
 * SelectModePage — 모드 선택 페이지
 *
 * @description
 * Pages Layer: SelectModeView feature를 조합한 완전한 페이지
 */

'use client';

import { SelectModeView } from '@/features/select-mode';

export function SelectModePage() {
  return <SelectModeView />;
}
